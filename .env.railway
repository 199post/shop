# ========================================
# RAILWAY ENVIRONMENT VARIABLES
# ========================================
# Скопируйте эти переменные в Railway Dashboard > Variables
# и замените значения на ваши реальные данные из Railway PostgreSQL

# Django Settings
DEBUG=0
SECRET_KEY=your-super-secret-key-here-minimum-50-characters-long
DJANGO_ALLOWED_HOSTS=*.railway.app
DJANGO_CSRF_TRUSTED_ORIGINS=https://*.railway.app

# Port (Railway автоматически устанавливает, но можно указать явно)
PORT=8000

# PostgreSQL Database (получите эти данные из Railway PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=your-database-password-from-railway
DB_HOST=your-database-host.railway.app
DB_PORT=5432

# ========================================
# ИНСТРУКЦИЯ ПО НАСТРОЙКЕ RAILWAY
# ========================================
# 
# 1. Создайте новый PostgreSQL в Railway:
#    - Откройте проект в Railway
#    - Нажмите "+ New" → "Database" → "Add PostgreSQL"
#    
# 2. Скопируйте credentials из PostgreSQL сервиса:
#    - Откройте PostgreSQL сервис
#    - Перейдите в "Variables" или "Connect"
#    - Скопируйте: PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
#
# 3. Установите переменные окружения в вашем Web сервисе:
#    - Откройте ваш Django сервис (не PostgreSQL)
#    - Перейдите в "Variables"
#    - Добавьте все переменные из секции выше
#    - Используйте значения из PostgreSQL для DB_* переменных
#
# 4. Deploy:
#    - Railway автоматически задеплоит после настройки переменных
#    - Procfile запустит миграции и seed_db автоматически
#    - Проверьте логи на успешность
#
# ========================================
