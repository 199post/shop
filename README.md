# PostgreSQL Migration - Railway & Local Setup

## ✅ Изменения завершены

Проект полностью переведен на **PostgreSQL** для локальной разработки и Railway.

### Что было сделано:

1. **Переменные окружения (`.env`)**
   - Добавлена переменная `PORT` для Railway
   - Настроены PostgreSQL credentials для локального Docker
   - `DEBUG=1` для локальной разработки

2. **Docker Compose (`docker-compose.yml`)**
   - ✨ Добавлен **Adminer** на порту `8080` для управления PostgreSQL
   - Улучшена конфигурация зависимостей (healthcheck)
   - Автоматический запуск миграций и seeding при старте

3. **Dockerfile**
   - Добавлены PostgreSQL клиент и библиотеки
   - Оптимизирован для production с gunicorn
   - Поддержка переменной `PORT` для Railway

4. **Requirements (`requirements.txt`)**
   - Добавлен `gunicorn>=21.0` для production WSGI server

5. **Railway Deploy (`Procfile`)**
   - Добавлена команда `seed_db` в release фазу
   - Автоматическое создание товаров при деплое

---

## 🚀 Локальный запуск

### Шаг 1: Запустить Docker Compose

```powershell
# Остановить и очистить старые контейнеры
docker-compose down -v

# Собрать и запустить
docker-compose up --build
```

### Шаг 2: Доступ к сервисам

- **Django**: http://localhost:8000
- **Adminer**: http://localhost:8080
- **Admin панель**: http://localhost:8000/admin (admin/admin)

### Шаг 3: Вход в Adminer

![Adminer Login](adminer_login.png)

Параметры подключения:
- **Система**: PostgreSQL
- **Сервер**: `db`
- **Имя пользователя**: `postgres`
- **Пароль**: `postgres`
- **База данных**: `shopdb`

---

## ☁️ Деплой на Railway

### Шаг 1: Создать PostgreSQL базу данных

1. Откройте ваш проект в Railway
2. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Дождитесь создания базы данных

### Шаг 2: Получить credentials

1. Откройте созданный PostgreSQL сервис
2. Перейдите в раздел **"Variables"** или **"Connect"**
3. Скопируйте следующие значения:
   - `PGHOST` → DB_HOST
   - `PGPORT` → DB_PORT (обычно 5432)
   - `PGUSER` → DB_USER (обычно postgres)
   - `PGPASSWORD` → DB_PASSWORD
   - `PGDATABASE` → DB_NAME (обычно railway)

### Шаг 3: Настроить переменные окружения

В вашем **Django сервисе** (не PostgreSQL), перейдите в **"Variables"** и добавьте:

```bash
DEBUG=0
SECRET_KEY=your-super-secret-key-here-minimum-50-characters
DJANGO_ALLOWED_HOSTS=*.railway.app
DJANGO_CSRF_TRUSTED_ORIGINS=https://*.railway.app

DB_ENGINE=django.db.backends.postgresql
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=<из PGPASSWORD>
DB_HOST=<из PGHOST>
DB_PORT=5432
```

> 💡 **Совет**: Используйте файл `.env.railway` как шаблон

### Шаг 4: Deploy

Railway автоматически задеплоит при изменении переменных или push в репозиторий.

**Procfile выполнит:**
1. `migrate` - создаст таблицы
2. `seed_db` - создаст 10 пользователей, 5 категорий, ~100 товаров
3. `collectstatic` - соберет статику
4. Запустит gunicorn сервер

### Шаг 5: Проверка

1. Откройте URL вашего приложения
2. Перейдите в `/admin`
3. Войдите: `admin` / `admin`
4. Проверьте наличие товаров в разделе "Products"

---

## 📊 База данных

### Что создается автоматически (seed_db):

- **Категории** (5 шт):
  - Электроника
  - Компьютеры  
  - Смартфоны
  - Аксессуары
  - Игровые устройства

- **Товары** (~100 шт):
  - Цены в гривнах (UAH)
  - Случайные названия и описания
  - Запасы от 0 до 150 единиц

- **Пользователи** (11 шт):
  - 10 обычных пользователей
  - 1 суперпользователь: `admin` / `admin`

### Повторный seeding

Команда `seed_db` проверяет наличие товаров перед созданием. Если товары уже есть - пропускает создание.

Для пересоздания данных:
```powershell
# Локально через Docker
docker-compose exec web python manage.py flush --noinput
docker-compose exec web python manage.py seed_db

# Или пересоздать все контейнеры
docker-compose down -v
docker-compose up --build
```

---

## 🔧 Troubleshooting

### Проблема: "relation does not exist"
**Решение**: Миграции не применены
```powershell
docker-compose exec web python manage.py migrate
```

### Проблема: Adminer не подключается к БД
**Решение**: Проверьте что сервер указан как `db` (имя сервиса), не `localhost`

### Проблема: Railway - Internal Server Error
**Решение**: Проверьте логи:
1. Откройте ваш сервис в Railway
2. Перейдите в "Deployments" → последний деплой → "View Logs"
3. Убедитесь что все переменные окружения установлены

### Проблема: Нет товаров после деплоя
**Решение**: Проверьте логи release фазы в Railway (должна быть команда seed_db)

---

## 📁 Структура файлов

```
shop/
├── .env                    # Локальные переменные окружения
├── .env.railway            # Шаблон для Railway (не коммитить!)
├── docker-compose.yml      # 3 сервиса: web, db, adminer
├── Dockerfile              # PostgreSQL + gunicorn
├── Procfile                # Railway: migrate + seed_db + collectstatic
├── requirements.txt        # Django, psycopg2, faker, gunicorn
└── src/
    ├── manage.py
    ├── config/
    │   ├── settings.py     # PostgreSQL настройки
    │   └── wsgi.py
    ├── store/
    │   ├── models.py       # Category, Product
    │   └── management/
    │       └── commands/
    │           └── seed_db.py  # Seeding скрипт
    └── accounts/
```
